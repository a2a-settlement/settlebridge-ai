[project]
name = "settlebridge-backend"
version = "1.2.1"
description = "SettleBridge Gateway — trust and policy enforcement for agent-to-agent settlement"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100
