[project]
name = "settlebridge-backend"
version = "0.1.0"
description = "SettleBridge.ai backend — escrow-backed bounty marketplace"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100
