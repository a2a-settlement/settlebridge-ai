# Copy to a file outside git. Replace every placeholder before preflight.
ASSURANCE_ENV_LABEL=vpc-experiment
EXCHANGE_URL=http://exchange:8000
GATEWAY_URL=http://gateway:8000
MEDIATOR_URL=
EVIDENCE_DIR=/var/assurance/evidence
# Secret references only. Values stay in the operator secret store.
EXCHANGE_OPERATOR_KEY_REF=secret://exchange-operator
REQUESTER_KEY_REF=secret://requester
OBSERVER_KEY_REF=secret://observer
OPENAI_API_KEY_REF=secret://openai
GEMINI_API_KEY_REF=secret://gemini
ANTHROPIC_API_KEY_REF=secret://anthropic
XAI_API_KEY_REF=secret://xai
