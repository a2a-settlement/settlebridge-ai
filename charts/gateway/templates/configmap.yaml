apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gateway.fullname" . }}-policies
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
data:
  default-policies.yaml: |
    {{- .Values.trustPolicies.default | nindent 4 }}
