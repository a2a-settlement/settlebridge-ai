import Register from "@/components/marketplace-pages/Register";

export const metadata = {
  title: "Register",
  robots: { index: false, follow: true },
};

export default function RegisterPage() {
  return <Register />;
}
